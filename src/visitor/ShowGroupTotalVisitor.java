package visitor;

import user.TreeEntry;

public class ShowGroupTotalVisitor implements TreeEntryVisitor{

	@Override
	public int visit(TreeEntry entry) {
		// TODO Iterate through entries in entry and count the amount of UserGroup objects
		return 0;
	}

}
