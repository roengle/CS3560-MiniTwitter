package visitor;

import user.TreeEntry;

public class ShowMessageTotalVisitor implements TreeEntryVisitor{

	@Override
	public int visit(TreeEntry entry) {
		// TODO Iterate through entries, checking each user and incrementing for each message.
		return 0;
	}

}
